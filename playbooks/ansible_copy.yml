---
- name: Copying files
  hosts: web1
  become: true
  tasks:
    - name: Copy a file to the remote server
      copy:
        src: /home/ansible/imp_file.txt
        dest: /var/
        owner: ansible
        group: ansible
        mode: '0644'
---
- name: Install NGInx
  hosts: all
  become: true
  tasks:
    - name: Ensure Nginx is at the latest version
      yum:
        name: nginx
        state: latest
    - name: start the nginx service
      service:
        name: nginx
        state: started
        enabled: true

  